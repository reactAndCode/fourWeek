# Weekly Log - 주간 작업일지

Next.js 16과 Supabase로 구축된 개인작업내역 관리 애플리케이션입니다.

## 📋 주요 기능

- **홈 대시보드**: 주간 작업 통계 및 요약
- **주간일지**: 일일 작업 내역 기록 및 관리
- **메모관리**: 업무 메모 작성 및 태그 분류

## 🛠 기술 스택

- **프레임워크**: Next.js 16.0.1 (App Router)
- **언어**: TypeScript 5
- **UI**: React 19.0.0
- **스타일링**: Tailwind CSS v4
- **컴포넌트**: Shadcn/ui
- **아이콘**: Lucide React
- **데이터베이스**: Supabase (PostgreSQL)
- **인증**: Supabase Auth

## 🚀 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. 환경 변수 설정

`.env.local` 파일을 생성하고 Supabase 프로젝트 정보를 입력하세요:

```bash
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

### 3. 데이터베이스 설정

`docs/데이터베이스_스키마.md` 파일의 SQL 스크립트를 Supabase SQL Editor에서 실행하세요.

테이블 생성 순서:
1. `my_weeklog` - 주간 작업일지 메인 테이블
2. `my_weeklog_task` - 작업 상세 항목
3. `my_memo` - 메모
4. RLS 정책 및 트리거

### 4. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

## 📁 프로젝트 구조

```
├── app/                    # Next.js App Router
│   ├── layout.tsx         # 루트 레이아웃
│   ├── page.tsx           # 홈 페이지
│   ├── weeklog/           # 주간일지
│   │   └── page.tsx
│   └── memos/             # 메모관리
│       └── page.tsx
│
├── components/            # React 컴포넌트
│   ├── ui/               # Shadcn/ui 기본 컴포넌트
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   └── badge.tsx
│   ├── navigation.tsx    # 네비게이션
│   └── weeklog/          # 주간일지 컴포넌트
│       ├── weekly-summary.tsx
│       ├── day-card.tsx
│       └── week-navigation.tsx
│
├── lib/                   # 유틸리티 및 비즈니스 로직
│   ├── utils.ts          # 공통 유틸리티
│   ├── db/               # 데이터베이스 레이어
│   │   ├── supabase.ts   # Supabase 클라이언트
│   │   └── server.ts     # 서버 컴포넌트용 클라이언트
│   ├── api/              # API 함수 레이어
│   └── bizlogic/         # 비즈니스 로직 레이어
│
├── types/                 # TypeScript 타입 정의
│   ├── database.types.ts # Supabase 데이터베이스 타입
│   └── worklog.types.ts  # 작업일지 타입
│
├── docs/                  # 문서
│   └── 데이터베이스_스키마.md
│
└── plan/                  # 기획 자료
    └── weeklog.png       # 화면 디자인
```

## 🗄 데이터베이스 스키마

### my_weeklog (주간 작업일지)

하루 단위의 작업 일지를 저장합니다.

- `work_date`: 작업 날짜
- `total_minutes`: 총 작업 시간(분)
- `status`: 작성 상태 (draft/completed/empty)

### my_weeklog_task (작업 항목)

일지의 개별 작업 항목을 저장합니다.

- `description`: 작업 설명
- `minutes`: 소요 시간(분)
- `category`: 카테고리 (design/planning/development/analysis)
- `tags`: 태그 배열

### my_memo (메모)

사용자의 메모를 저장합니다.

- `title`: 제목
- `content`: 내용
- `tags`: 태그 배열
- `is_pinned`: 상단 고정 여부

자세한 스키마 정보는 `docs/데이터베이스_스키마.md`를 참조하세요.

## 📝 개발 명령어

```bash
# 개발 서버 실행
npm run dev

# 프로덕션 빌드
npm run build

# 프로덕션 서버 실행
npm start

# 린터 실행
npm run lint
```

## 🎨 UI 컴포넌트

프로젝트는 [Shadcn/ui](https://ui.shadcn.com/)를 사용합니다. 새 컴포넌트 추가:

```bash
npx shadcn-ui@latest add [component-name]
```

## 📖 다중티어 아키텍처

프로젝트는 관심사 분리를 위한 다중티어 패턴을 따릅니다:

1. **UI Layer** (`components/`): React 컴포넌트
2. **Business Logic Layer** (`lib/bizlogic/`): 비즈니스 로직
3. **API Layer** (`lib/api/`): 데이터 CRUD 함수
4. **Database Layer** (`lib/db/`): Supabase 클라이언트

## 🔐 인증

Supabase Auth를 사용하여 사용자 인증을 관리합니다.

- Row Level Security (RLS)로 데이터 접근 제어
- 각 사용자는 자신의 데이터만 접근 가능

## 🚧 TODO

- [ ] Supabase Auth 인증 구현
- [ ] 작업 생성/수정 모달 구현
- [ ] 실제 데이터베이스 연동
- [ ] 메모 CRUD 기능 구현
- [ ] 한국어 날짜 포매팅
- [ ] 주간 통계 계산 로직
- [ ] 파일 업로드 (Supabase Storage)

## 📚 참고 문서

- [Next.js 문서](https://nextjs.org/docs)
- [Supabase 문서](https://supabase.com/docs)
- [Tailwind CSS 문서](https://tailwindcss.com/docs)
- [Shadcn/ui 문서](https://ui.shadcn.com/)

## 📄 라이선스

MIT
